# Normalize text to LF in the repo so the Linux/Render build (Dockerfile, shell
# scripts, Python) never sees CRLF. Binary assets are left untouched.
* text=auto eol=lf

*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.woff  binary
*.woff2 binary
*.mp3  binary
*.rar  binary
