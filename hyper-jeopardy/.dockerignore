# Sibling apps — each has its own Render service / Dockerfile.
# Excluded here so the Jeopardy build (whose context is the repo root)
# doesn't try to type-check or compile their files.
hive/
wof/
hub/
catan-app/

# Standard ignores
node_modules
.next
.git
.github
*.tsbuildinfo
.env*
.DS_Store
*.rar
data/*.db
data/*.db-shm
data/*.db-wal
